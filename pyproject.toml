[project]
name = "postmodern"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
postmodern = "postmodern.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
