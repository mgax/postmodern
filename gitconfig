[pretty]
	postmodern = %C(yellow)%h%C(reset) %C(blue)%an %C(green)%ad%C(red)%d%C(reset)%n  %s
[core]
	editor = nvim
	pager = delta
[delta]
	navigate = true
	dark = true
[interactive]
	diffFilter = delta --color-only
[merge]
	conflictStyle = zdiff3
[init]
	defaultBranch = main
